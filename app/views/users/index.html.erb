<h1><%= User.model_name.human %>一覧</h1>
<ul>
  <% @users.each do |user| %>
    <li><a href="users/<%= user.id %>"><%= user.name %></li>
  <% end %>
</ul>

<%= paginate @users %>

<br>

<%= link_to t('views.common.back'), :back %>