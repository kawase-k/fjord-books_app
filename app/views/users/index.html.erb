<h1><%= User.model_name.human %>一覧</h1>
<ul>
  <% @users.each do |user| %>
    <li><a href="users/<%= user.id %>"><%= user.name %></li>
  <% end %>
</ul>

<%= paginate @users %>

<br>

<%= link_to t('devise.shared.links.back'), :back %>
<%= link_to '本一覧へ', books_path %>